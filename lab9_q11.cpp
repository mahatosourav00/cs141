/*
* Auto-Generated File
* Created Using cpp_file_manager
*/

// Importing Libraries
#include <iostream>

using namespace std;

// Main Function
int main(){
	// To-Do
	// variables
	bool b = true;
	char c = 'a';	
	int i = 1;
	float f = 1.0;
	double d = 1.0;
	long l = 1;
	// pointers
	bool *pb = &b;
	char *pc = &c;
	int *pi = &i;
	float *pf = &f;
	double *pd = &d;
	long *pl = &l;
	//output
	cout << "bool \tvar size = "<<sizeof(b)<<"\tptr size = "<<sizeof(pb)<< "\nint \tvar size = "<<sizeof(i)<<"\tptr size = "<<sizeof(pi)<< "\nchar \tvar size = "<<sizeof(c)<<"\tptr size = "<<sizeof(pc)<< "\nfloat \tvar size = "<<sizeof(f)<<"\tptr size = "<<sizeof(pf)<< "\nlong \tvar size = "<<sizeof(l)<<"\tptr size = "<<sizeof(pl)<< "\ndouble \tvar size = "<<sizeof(d)<<"\tptr size = "<<sizeof(pd)<<endl; 
	return 0;
}


